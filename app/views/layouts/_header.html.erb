<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<%= link_to 'Book Review', root_path, class: 'navbar-brand' %>
		</div>
		<ul class="nav navbar-nav">
			<li><%= link_to 'Sign Up', new_user_registration_path %></li>
			<% if user_signed_in? %>
				<li><%= link_to('Logout', destroy_user_session_path, method: :delete) %> </li>
			<% else %>
				<li><%= link_to('Login', new_user_session_path)  %></li>
			<% end %>
		</ul>
		<ul class="nav navbar-nav navbar-right">
			<li class="dropdown">
				<button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Book Category
				<span class="caret"></span></button>
				<ul class="dropdown-menu" role="menu">
					<% Category.all.each do |category| %>
						<li>
							<%= link_to category.name, books_path(category: category.name), class: "link" %>
						</li>
					<% end %>
				</ul>
			</li>
			<li class="btn btn-success"><%= link_to 'Add new Book', new_book_path %></li>
		</ul>
	</div>
</nav>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>